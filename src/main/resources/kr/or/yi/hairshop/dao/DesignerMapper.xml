<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.yi.hairshop.dao.DesignerMapper">
	
	<select id="selectDesignerByAll" resultType="Designer">
		select * from Designer
	</select>
	<!-- <select id="selectByLimit" resultType="Designer">
		select *
		from Designer
		order by d_no asc limit #{startRow},#{size}
	</select> -->
	
	<insert id="insertDesigner" parameterType="Designer">
		insert into designer (d_grade, d_name, d_tel, d_tel2, d_addr, d_addr2, d_addr3, d_birth, d_join, d_memo) 
		values (#{dGrade}, #{dName}, #{dTel}, #{dTel2}, #{dAddr}, #{dAddr2}, #{dAddr3}, #{dBirth}, #{dJoin}, #{dMemo})
	</insert>
	
	<delete id="deleteDesigner">
		delete from designer where d_no=#{dNo}		
	</delete>
	
	<update id="updateDesigner">
		UPDATE designer
		SET d_grade=#{dGrade}, d_name=#{dName}, d_tel=#{dTel}, d_tel2=#{dTel2}, d_addr=#{dAddr}, d_addr2=#{dAddr2}, d_addr3=#{dAddr3}, d_birth=#{dBirth}, d_join=#{dJoin}, d_memo=#{dMemo}
		WHERE d_no=#{dNo}	
	</update>
	
</mapper>